# Example helmfile release.
# A release is autogenerated with the install wizard.

# releases:
  # - name: openad-model
  #   namespace: openad-models
  #   chart: ./helm
    # values:
    #   - values.yaml  # override file values.yaml
    # # override specific values in values.yaml
    # set:
    #   - name: fullnameOverride  # override the name of the app
    #     value: 
    #   - name: nameOverride  # override the name of the app
    #     value: 

